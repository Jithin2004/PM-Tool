# Resolve PM Database

This directory contains the database schema and definitions for Resolve PM.

## Canonical Installation

1. Create a Supabase project
2. Execute the master SQL file: `production/RESOLVE_PM_V1_3_INSTALL.sql`
3. Do not run archived migrations

*Note: All previous individual migrations have been merged into the master install file and archived under `archive/merged_migrations/`.*
