import React from 'react';

export default function DocumentView() {
  return (
    <div className="p-6">
      <div className="border border-white/10 bg-white/[0.02] p-12 flex flex-col items-center justify-center text-center">
        <span className="text-[11px] font-mono text-white/30 uppercase tracking-wider">Document Viewer</span>
        <span className="text-[9px] font-mono text-white/20 mt-2">Document rendering, annotations, version history — coming soon</span>
      </div>
    </div>
  );
}
